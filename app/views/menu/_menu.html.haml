%nav#main-menu
  %ul#menu
    %li
      = link_to t('.all_images'), root_path
    %li
      = link_to t('.categories') ,root_path
      %ul
        - @categories.each do |category|
          %li
            = link_to category_path(category.id) do
              = category.name.capitalize
              %small
                (
                = category.images_count
                )

    %li
      = link_to t('.events'), events_path