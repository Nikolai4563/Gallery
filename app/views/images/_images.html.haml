- @images.each do |image|
  .image-block
    = link_to image_tag(image.url.url(:thumb)), image_path(image.id), :id => "image-#{image.id}"
    .image-info
      = image.likes_count
      %i.icon-thumbs-up
      = image.comments_count
      %i.icon-comment