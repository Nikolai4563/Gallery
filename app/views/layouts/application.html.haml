!!!
%html
  %head
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
      %title= content_for?(:title) ? yield(:title) : "Gallery"
      %meta{:content => content_for?(:description) ? yield(:description) : "Gallery", :name => "description"}
        = stylesheet_link_tag "application", :media => "all"
        = javascript_include_tag "application"
        = csrf_meta_tags
        = yield(:head)
  %body{:class => "#{controller_name} #{action_name}"}
    #pusher-comment
      %i.icon-remove.pull-right
      .pusher-content
    - if current_user
      #live-chat
        = image_tag '/liveChat.png', :class => 'img-button'
        .chat-block
    .navbar.navbar-fixed-top
      %nav.navbar-inner
        .container
          = render 'layouts/navigation'
          = link_to '/auth/facebook', :class => 'facebook pull-right' do
            = image_tag '/login-with-facebook.png', :alt => 'Facebook'
    #main{:role => "main"}
      .container
        .content
          .row
            .span12
              = render 'layouts/messages'
              = render 'menu/menu'
              = yield
    %footer
      .container
        %p
          Designed and built with all the love in the world by Nikolai